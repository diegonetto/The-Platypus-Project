<%= form_for(@search_list) do |f| %>
  <% if @search_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@search_list.errors.count, "error") %> prohibited this search_list from being saved:</h2>

      <ul>
      <% @search_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
